## RAG 检索增强生成

RAG 就是让模型用你的私有资料回答。五步：分块、embedding、建索引、检索、注入 prompt。
把文档切成小块，每块转成向量，问题也转成向量，找最相近的几块喂给模型。
它没读整本书，却能答得又快又准，还能给出处。

## 混合检索与 RRF

只用向量检索会漏掉专有名词，只用关键词检索又抓不到语义。所以用混合检索：
向量索引和 BM25 索引同时搜，各自给出排名，再用 RRF 融合。
RRF 的分数是 1/(k+rank) 累加，两个列表都排前面的块得分最高。
